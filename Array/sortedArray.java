package Array;

public class sortedArray {
    public static void isSorted(int[] args) {
        int[]arr=Arrays10 input();
        for(int i=1;i<arr.length;i++){
            if(arr[i]>=arr[i-1]{

            }else{
                return false;
            }

            )
            return true;
        }

        
    }
    
}
